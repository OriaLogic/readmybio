import React, { Component, PropTypes } from 'react';

export default class UserPresentation extends Component {
  render () {
    return (
      <div>
        USER PRESENTATIOn
      </div>
    );
  }
}
